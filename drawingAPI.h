#include "context.h"


void draw_point(Context c, unsigned int x, unsigned int y, unsigned short color);
void fill_rect(Context c, unsigned int bx, unsigned int by, unsigned int width, unsigned int height, unsigned short color);
void draw_character(Context c, unsigned int bx, unsigned int by, char c);
